# Flask Environment Configuration
# Copy this file to .env and customize as needed

# Flask application settings
FLASK_ENV=development
FLASK_DEBUG=1

# Docker environment flag
DOCKER_ENV=1

# Port configuration (change if 5000 is in use)
# Update docker-compose.yml ports section to match
HOST_PORT=5000
CONTAINER_PORT=5000
